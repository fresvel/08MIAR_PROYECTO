services:
  rl:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: miar-rl:py38
    working_dir: /workspace
    volumes:
      - .:/workspace:Z
    stdin_open: true
    tty: true
    command: bash
